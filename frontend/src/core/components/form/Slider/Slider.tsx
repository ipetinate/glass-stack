import { useId, type InputHTMLAttributes, type ReactNode } from 'react'
import { cn } from '@/core/functions/class-name'
export type SliderProps = Omit<InputHTMLAttributes<HTMLInputElement>, 'type' | 'size'> & { label?: ReactNode; helperText?: ReactNode; error?: ReactNode; errorText?: ReactNode; containerClassName?: string }
export function Slider({ label, helperText, error, errorText, containerClassName, className, id: providedId, ...props }: SliderProps) { const id = providedId ?? useId(); const message = error ?? errorText; return <label htmlFor={id} className={cn('block w-full space-y-2', containerClassName)}>{label ? <span className="block text-sm text-black/75 dark:text-white/80">{label}</span> : null}<input {...props} id={id} type="range" className={cn('h-2 w-full cursor-pointer accent-cyan-400', className)} />{message ? <span className="block text-xs text-rose-500">{message}</span> : helperText ? <span className="block text-xs text-black/55 dark:text-white/55">{helperText}</span> : null}</label> }
